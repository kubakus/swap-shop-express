{
  "name": "swapshop-db-api",
  "version": "1.0.0",
  "description": "Database api for SwapShop project",
  "main": "index.js",
  "repository": "git@bitbucket.org:Jakub_Kus/swapshop-db-api.git",
  "author": "Jakub Kus (kuskuba@gmail.com)",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "tsc",
    "mongo:build": "mkdir -p dev/data && docker-compose -f dev/docker-compose.yml up -d",
    "mongo:stop": "docker-compose -f dev/docker-compose.yml down"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^14.14.25",
    "typescript": "^4.1.5"
  },
  "peerDependencies": {
    "tslib": "^2.1.0"
  }
}
