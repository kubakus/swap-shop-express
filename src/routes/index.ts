import { Router } from "express";

export class ApiRouter {
    public static async createRouter(): Promise<Router> {
        const router = Router()
        // TODO Add routes/subrouters here
        
        return router
    }
}